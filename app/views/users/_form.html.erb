<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name_user %>
    <%= form.text_field :name_user %>
  </div>

  <div class="field">
    <%= form.label :avatar_user %>
    <%= form.text_field :avatar_user %>
  </div>

  <div class="field">
    <%= form.label :name_pantry_user %>
    <%= form.text_field :name_pantry_user %>
  </div>

  <div class="field">
    <%= form.label :address_user %>
    <%= form.text_field :address_user %>
  </div>

  <div class="field">
    <%= form.label :country_user %>
    <%= form.text_field :country_user %>
  </div>

  <div class="field">
    <%= form.label :phone_user %>
    <%= form.number_field :phone_user %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
