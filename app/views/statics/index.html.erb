<p id="notice"><%= notice %></p>

<h1>Statics</h1>

<table>
  <thead>
    <tr>
      <th>Id statics</th>
      <th>Id menu</th>
      <th>Count menu</th>
      <th>Score menu</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @statics.each do |static| %>
      <tr>
        <td><%= static.id_statics %></td>
        <td><%= static.id_menu %></td>
        <td><%= static.count_menu %></td>
        <td><%= static.score_menu %></td>
        <td><%= link_to 'Show', static %></td>
        <td><%= link_to 'Edit', edit_static_path(static) %></td>
        <td><%= link_to 'Destroy', static, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Static', new_static_path %>
